services:
  dbpostgres:
    image: postgres:18-alpine
    container_name: db
    restart: always
    environment:
      - POSTGRES_PASSWORD=P@ssw0rd
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: